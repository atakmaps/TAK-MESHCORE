package com.atakmaps.meshcore.plugin.protocol;

/**
 * Identifies which link delivered an inbound ping (MeshCore RF or TAK/Wi‑Fi).
 */
public enum RfInboundTransport {
    MESHCORE,
    WIFI
}
